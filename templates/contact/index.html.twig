{% extends 'base.html.twig' %}

{% block title %}Nous contacter - Lady fantastique{% endblock %}

{% block content %}
    <div class="bg-white border py-3 mb-3">
        <div class="row">
            <div class="col-md-12 ml-3">
                <a href="{{ path('home') }}" class="text-decoration-none"> Accueil</a>
                <span class="mx-2 mb-0">/</span>
                <a href="{{path('contact')}}" class="text-decoration-none ">Contact</a></div>
        </div>
    </div>
    <h1 class="h2 text-uppercase">Nous contacter</h1>
    <p>N'hésitez pas à prendre contact avec notre équipe, nous vous répondrons dans les meilleurs délais.</p>
    {% for message in app.flashes('notice') %}
        <div class="alert alert-info">{{ message }}</div>
    {% endfor %}
    <div class="row">
        <div class="col-md-8">{{ form(form)}}</div>
        <div class="col-md-4">
            <h2>Lady Fantastique</h2>
            <hr>
            <p class="text-danger">Pour toute commande spécifique, indiquez les détails dans la partie message.</p>
            <p>Marseille<br>
            ladyfantastique@free.fr</p>
        </div>
    </div>

{% endblock %}
